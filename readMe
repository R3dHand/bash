BASH
    -git_manager
        -Easily manage git repositories
        -Manage repositories from WSL (Windows Sub-system for Linux)
    -node_setup
        -easily create a node project
        -install essential node modules 
        -add appropriate node scripts to make use of modules
        -create dir for css and js and other needed directories
        -generate index.html
        -link html use created css and js
        -open dev server and browser to test succesful creation of project
